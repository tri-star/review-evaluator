AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: Review evaluator Lambda and related infrastructure.

Parameters:
  AppName:
    Type: String
    Default: review-evaluator
  CreateReviewDataBucket:
    Type: String
    AllowedValues:
      - "true"
      - "false"
    Default: "false"
  ReviewDataBucketName:
    Type: String
    Default: ""
    Description: Existing bucket name, or managed bucket name when CreateReviewDataBucket=true.
  IntegrationsSecretArn:
    Type: String
    Default: ""
    Description: ARN of Secrets Manager secret containing GitHub, Slack, and webhook integration values.
  BotName:
    Type: String
    Default: review-bot
    Description: GitHub bot login without @ used for review commands.
  RuleGeneratorProvider:
    Type: String
    Default: openai
    AllowedValues:
      - openai
      - anthropic
    Description: LLM provider used to extract review rules.
  AnthropicModel:
    Type: String
    Default: claude-haiku-4-5
    Description: Anthropic model id used when RuleGeneratorProvider=anthropic.
  OpenAiModel:
    Type: String
    Default: gpt-5.4-mini
    Description: OpenAI model id used when RuleGeneratorProvider=openai.
  SummaryIssueNumber:
    Type: String
    Default: ""
  RepoFullName:
    Type: String
    Default: ""
    Description: owner/repo
  AthenaWorkGroupName:
    Type: String
    Default: ""
    Description: Optional external Athena workgroup name.
  AthenaDatabaseName:
    Type: String
    Default: ""
    Description: Optional external Athena/Glue database name.
  AthenaResultsBucketArn:
    Type: String
    Default: ""
    Description: Optional external Athena results bucket ARN.
  LogLevel:
    Type: String
    Default: INFO
    AllowedValues:
      - DEBUG
      - INFO
      - WARNING
      - ERROR
    Description: Lambda log level. Use DEBUG to trace per-PR evaluation details.

Conditions:
  UseManagedBucket: !Equals [!Ref CreateReviewDataBucket, "true"]
  HasManagedBucketName: !And
    - !Condition UseManagedBucket
    - !Not [!Equals [!Ref ReviewDataBucketName, ""]]
  HasIntegrationsSecret: !Not [!Equals [!Ref IntegrationsSecretArn, ""]]

Globals:
  Function:
    Runtime: python3.12
    Timeout: 120
    MemorySize: 256
    Architectures:
      - x86_64
    Environment:
      Variables:
        REVIEW_DATA_BUCKET: !If
          - UseManagedBucket
          - !Ref ReviewDataBucket
          - !Ref ReviewDataBucketName
        REPO_FULL_NAME: !Ref RepoFullName
        SUMMARY_ISSUE_NUMBER: !Ref SummaryIssueNumber
        ATHENA_WORKGROUP_NAME: !Ref AthenaWorkGroupName
        ATHENA_DATABASE_NAME: !Ref AthenaDatabaseName
        ATHENA_RESULTS_BUCKET_ARN: !Ref AthenaResultsBucketArn
        POWERTOOLS_SERVICE_NAME: review-evaluator
        POWERTOOLS_LOG_LEVEL: !Ref LogLevel

Resources:
  ReviewDataBucket:
    Type: AWS::S3::Bucket
    Condition: UseManagedBucket
    Properties:
      BucketName: !If [HasManagedBucketName, !Ref ReviewDataBucketName, !Ref "AWS::NoValue"]
      VersioningConfiguration:
        Status: Enabled

  ReviewEvaluatorFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/review_evaluator/
      Handler: app.lambda_handler
      Description: Evaluate AI review results and publish daily summaries.
      Policies:
        - S3CrudPolicy:
            BucketName: !If
              - UseManagedBucket
              - !Ref ReviewDataBucket
              - !Ref ReviewDataBucketName
        - Statement:
            - !If
              - HasIntegrationsSecret
              - Effect: Allow
                Action:
                  - secretsmanager:GetSecretValue
                Resource: !Ref IntegrationsSecretArn
              - !Ref AWS::NoValue
      Environment:
        Variables:
          INTEGRATIONS_SECRET_ARN: !Ref IntegrationsSecretArn
      Events:
        DailySchedule:
          Type: ScheduleV2
          Properties:
            ScheduleExpression: cron(0 21 * * ? *)
            ScheduleExpressionTimezone: Asia/Tokyo

  RuleExtractionDLQ:
    Type: AWS::SQS::Queue
    Properties:
      MessageRetentionPeriod: 1209600

  RuleExtractionQueue:
    Type: AWS::SQS::Queue
    Properties:
      VisibilityTimeout: 180
      RedrivePolicy:
        deadLetterTargetArn: !GetAtt RuleExtractionDLQ.Arn
        maxReceiveCount: 3

  ReviewCommandFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/review_evaluator/
      Handler: app.handle_review_command
      Description: Handle GitHub App webhooks (review commands and pull_request events).
      Policies:
        - SQSSendMessagePolicy:
            QueueName: !GetAtt RuleExtractionQueue.QueueName
        - Statement:
            - !If
              - HasIntegrationsSecret
              - Effect: Allow
                Action:
                  - secretsmanager:GetSecretValue
                Resource: !Ref IntegrationsSecretArn
              - !Ref AWS::NoValue
      Environment:
        Variables:
          INTEGRATIONS_SECRET_ARN: !Ref IntegrationsSecretArn
          BOT_NAME: !Ref BotName
          RULE_EXTRACTION_QUEUE_URL: !Ref RuleExtractionQueue
      Events:
        ReviewCommandWebhook:
          Type: HttpApi
          Properties:
            Path: /github/review-command
            Method: POST

  RuleExtractorFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/review_evaluator/
      Handler: app.process_rule_extraction
      Description: Extract reusable review rules from closed pull requests.
      Timeout: 180
      Policies:
        - S3CrudPolicy:
            BucketName: !If
              - UseManagedBucket
              - !Ref ReviewDataBucket
              - !Ref ReviewDataBucketName
        - Statement:
            - !If
              - HasIntegrationsSecret
              - Effect: Allow
                Action:
                  - secretsmanager:GetSecretValue
                Resource: !Ref IntegrationsSecretArn
              - !Ref AWS::NoValue
      Environment:
        Variables:
          INTEGRATIONS_SECRET_ARN: !Ref IntegrationsSecretArn
          RULE_GENERATOR_PROVIDER: !Ref RuleGeneratorProvider
          ANTHROPIC_MODEL: !Ref AnthropicModel
          OPENAI_MODEL: !Ref OpenAiModel
      Events:
        RuleExtractionJob:
          Type: SQS
          Properties:
            Queue: !GetAtt RuleExtractionQueue.Arn
            BatchSize: 1

Outputs:
  ReviewEvaluatorFunctionArn:
    Value: !GetAtt ReviewEvaluatorFunction.Arn
  ReviewDataBucketOutput:
    Value: !If
      - UseManagedBucket
      - !Ref ReviewDataBucket
      - !Ref ReviewDataBucketName
  ReviewCommandWebhookUrl:
    Value: !Sub "https://${ServerlessHttpApi}.execute-api.${AWS::Region}.${AWS::URLSuffix}/github/review-command"
